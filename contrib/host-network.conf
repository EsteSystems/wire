# wire network configuration
# Primary network configuration for this host
# Managed by wire daemon - do not edit while daemon is running

# Primary interface
interface enp0s5 set state up
interface enp0s5 address 10.0.0.20/24

# Default gateway
route default via 10.0.0.1

# Static route to test network
route 10.10.100.0/24 via 10.0.0.1
