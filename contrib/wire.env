# Wire daemon configuration
# Place this file at /etc/wire/wire.env

# Configuration file to use
WIRE_CONFIG=/etc/wire/network.conf

# Daemon options (--verbose, --dry-run, or both)
WIRE_OPTIONS=--verbose
